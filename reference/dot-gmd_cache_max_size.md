# Maximum number of cached entries (LRU eviction when exceeded)

Maximum number of cached entries (LRU eviction when exceeded)

## Usage

``` r
.gmd_cache_max_size
```

## Format

An object of class `integer` of length 1.
