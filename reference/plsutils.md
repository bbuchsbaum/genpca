# Internal Utilities for Partial Eigen, Adaptive Rank, and Sqrt Transforms

These functions are used by both `genpls` and `genplscorr` to handle
partial eigen expansions, diagonal/identity shortcuts, adaptive rank
selection, and row/column transformations for data embeddings.
