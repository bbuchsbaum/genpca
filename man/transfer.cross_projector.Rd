% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transfer_wrappers.R
\name{transfer.cross_projector}
\alias{transfer.cross_projector}
\title{Compatibility wrapper for multivarious::transfer}
\usage{
\method{transfer}{cross_projector}(
  x,
  new_data,
  from = NULL,
  to = NULL,
  source = NULL,
  target = NULL,
  opts = list(),
  ...
)
}
\arguments{
\item{x}{A cross_projector object.}

\item{new_data}{New data to transfer.}

\item{from}{Source space ("X" or "Y").}

\item{to}{Target space ("X" or "Y").}

\item{source}{Legacy parameter name for `from`. Deprecated, use `from` instead.}

\item{target}{Legacy parameter name for `to`. Deprecated, use `to` instead.}

\item{opts}{Options list passed to multivarious::transfer.}

\item{...}{Additional arguments passed through.}
}
\value{
Matrix with transferred data.
}
\description{
Provides a transfer method that accepts `source`/`target` arguments
used in the tests. The method forwards to `multivarious`'s
transfer method which expects `from`/`to`.
}
\examples{
\donttest{
# Generate sample data
set.seed(123)
n <- 50
X <- matrix(rnorm(n * 10), n, 10)
Y <- matrix(rnorm(n * 8), n, 8)

# Create a cross projector using rpls
fit <- rpls(X, Y, K = 2)

# Transfer new X data to Y space
new_X <- matrix(rnorm(10 * 10), 10, 10)
transferred <- transfer(fit, new_X, from = "X", to = "Y")
}

}
